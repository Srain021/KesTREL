---
id: RFC-001
title: Lock dependencies with uv
epic: A-Foundations
status: open
owner: unassigned
role: backend-engineer
blocking_on: []
budget:
  max_files_touched: 4
  max_new_files: 1
  max_lines_added: 40
  max_minutes_human: 15
  max_tokens_model: 8000
files_to_read:
  - pyproject.toml
  - README.md
files_will_touch:
  - pyproject.toml          # modified
  - uv.lock                 # new
  - .gitignore              # modified
  - README.md               # modified
verify_cmd: |
  .venv\Scripts\python.exe -c "import uv" 2>nul && .venv\Scripts\python.exe -m uv --version && .venv\Scripts\python.exe -c "from pathlib import Path; assert Path('uv.lock').is_file(), 'uv.lock missing'; print('lock present', Path('uv.lock').stat().st_size, 'bytes')"
rollback_cmd: |
  git checkout -- pyproject.toml .gitignore README.md
  if exist uv.lock del uv.lock
skill_id: rfc-001-uv-lock
---

# RFC-001 — Lock dependencies with uv

## Mission

把依赖从 `pip install >=X` 改成 `uv` 锁定，确保可复现构建。

## Context

- 当前 `pyproject.toml` 用 `>=` 范围，两个开发者装同一 commit 可能得不同版本。
- GAP_ANALYSIS G-E3 明确列为 P0。
- CI（RFC-002）要用 `uv sync --frozen` 跑；没有锁文件就没 CI。
- `uv` 比 pip 快 10-100 倍，是 2024+ Python 生态事实标准。

## Non-goals

- 不迁移到 `pep621` 之外的格式（pyproject.toml 本就是 PEP 621）。
- 不删除 `pip install -e .` 的支持路径 —— 保持向后兼容。
- 不重排依赖（保持当前分组：主依赖 + `[dev]`）。

## Design

1. 用 `pip install uv` 装 uv 到现有 venv（不破坏现有环境）。
2. 跑 `uv lock` 生成 `uv.lock`。
3. 更新 `pyproject.toml` 加 `[tool.uv]` 段落（明确 indexer 用清华，和当前一致）。
4. `.gitignore` 不忽略 `uv.lock`（锁文件必须入库）。
5. README 加一节 "Reproducible installs" 说明 `uv sync` 用法。

## Steps

### Step 1 — 装 uv 到现有 venv

```
RUN .venv\Scripts\python.exe -m pip install --upgrade "uv>=0.5"
```

### Step 2 — pyproject.toml 加 uv 段

```
REPLACE pyproject.toml
<<<<<<< SEARCH
[project.urls]
Homepage = "https://github.com/your-org/redteam-mcp"
Documentation = "https://github.com/your-org/redteam-mcp#readme"
Issues = "https://github.com/your-org/redteam-mcp/issues"
=======
[project.urls]
Homepage = "https://github.com/your-org/redteam-mcp"
Documentation = "https://github.com/your-org/redteam-mcp#readme"
Issues = "https://github.com/your-org/redteam-mcp/issues"

[tool.uv]
default-groups = ["dev"]

[[tool.uv.index]]
name = "tsinghua"
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
>>>>>>> REPLACE
```

### Step 3 — 生成 uv.lock

```
RUN .venv\Scripts\python.exe -m uv lock
```

(预期: 生成约 2000–4000 行的 `uv.lock` 文件，退出码 0)

### Step 4 — 用 uv sync 验证可复现安装（副作用最小）

```
RUN .venv\Scripts\python.exe -m uv sync --frozen
```

### Step 5 — 更新 .gitignore

```
REPLACE .gitignore
<<<<<<< SEARCH
.env
.env.local
.env.*.local
=======
.env
.env.local
.env.*.local
# NOTE: uv.lock is DELIBERATELY tracked — see RFC-001
>>>>>>> REPLACE
```

### Step 6 — README 加段落

```
REPLACE README.md
<<<<<<< SEARCH
Or manually with any Python 3.10+:

```bash
pip install -e ".[dev]"
```
=======
Or manually with any Python 3.10+:

```bash
pip install -e ".[dev]"
```

### Reproducible installs (recommended)

We use [uv](https://github.com/astral-sh/uv) to pin every transitive dependency.
The committed `uv.lock` file guarantees byte-identical installs across
developers, CI, and production.

```bash
pip install uv
uv sync --frozen
```

See [RFC-001](./rfcs/RFC-001-uv-lock-dependencies.md) for the rationale.
>>>>>>> REPLACE
```

### Step 7 — 确认 verify_cmd 绿 + full_verify 绿

```
RUN .venv\Scripts\python.exe scripts\full_verify.py
```

## Tests

本 RFC 不引入新单元测试（pure tooling change）。verify_cmd 本身已断言 `uv.lock` 存在。

## Post-checks

- [ ] `git diff --stat` 仅列出 `pyproject.toml` / `.gitignore` / `README.md`，加上新增的 `uv.lock`
- [ ] `uv.lock` 头部注释标识它是 uv 生成（第一行 `# This file was autogenerated by uv via the following command:`）
- [ ] 新开一个 shell：`.venv\Scripts\python.exe -m pytest tests/ -q` 仍能跑（95 passed）

## Rollback plan

```
git checkout -- pyproject.toml .gitignore README.md
if exist uv.lock del uv.lock
```

## Updates to other docs

- `CHANGELOG.md` 的 `[Unreleased]` 段加：`RFC-001 closes GAP G-E3 (deps locked via uv)`
- `GAP_ANALYSIS.md` 里 G-E3 状态从 `P0 待修` 改成 `DONE (RFC-001)`

## Notes for executor

- 如果 Step 3 的 `uv lock` 报 "failed to solve"，大概率是某个包版本不兼容；不要改 pyproject.toml 兜底，直接 rollback 并把 stderr tail 写进 blocked 记录。
- `uv.lock` 必须 commit 进仓库。不要让它进 .gitignore。

## Changelog

- **2026-04-21 初版** — Spec Author: Claude pair
